# URL of the deployed/running backend API.
# Leave this UNSET (or blank) while you don't have a backend running yet —
# the site will automatically use the placeholder data in src/data/siteData.ts instead.
VITE_API_BASE_URL=http://localhost:5000
