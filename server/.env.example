# MongoDB connection string (MongoDB Atlas free tier works great).
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/al-ameen-portfolio

# Port the API server runs on.
PORT=5000

# Your deployed frontend URL, used for CORS. Use * while testing locally.
FRONTEND_URL=*
